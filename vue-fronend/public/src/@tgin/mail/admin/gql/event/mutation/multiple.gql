#import "../fragment/EventAdmin.gql"

mutation(
  $ids: [Int],
  $action: String,
  $detail: Boolean = false
)
{
  res: mail_admin_events(
    ids: $ids,
    action: $action
  ) {
    ...MailEventAdmin
  }
}
