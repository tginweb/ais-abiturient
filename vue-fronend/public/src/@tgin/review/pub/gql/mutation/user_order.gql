#import "@tgin/review/core/gql/fragments/Review.gql"

mutation(
    $model: Json,
    $orderId: Int,
    $elementInclude: Boolean = true
)
{
    res: review_pub_user_order(orderId: $orderId, model: $model) {
        ...Review
    }
}

