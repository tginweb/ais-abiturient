#import "@tgin/sale/core/gql/order/fragments/Order.gql"

mutation(
    $id: Int,
    $hash: String,
    $comment: String = "",
    $orderDetail: Boolean = true
)
{
    res: sale_pub_order_cancel(id: $id, comment: $comment) {
        ...Order
    }
}
