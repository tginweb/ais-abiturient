#import "@tgin/main/graphql/fragment/Response.gql"

mutation(
    $paymentId: Int,
    $orderId: Int
)
{
  res: ps_yookassa_createItem(
    paymentId: $paymentId,
    orderId: $orderId
  ) {
    ...Response
  }
}
