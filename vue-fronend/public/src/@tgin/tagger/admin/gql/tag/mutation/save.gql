#import "@tgin/main/graphql/fragment/ResultWithPayload.gql"

mutation(
    $id: String,
    $action: String,
    $model: JSON
) {
    res: tagger_admin_tag_save(
        id: $id,
        action: $action,
        model: $model
    ) {
        ...ResultWithPayload
    }
}


