#import "@tgin/main/graphql/fragment/ResultWithPayload.gql"

mutation(
  $action: String,
  $ids: [Int],
  $models: JSON
) {
   res: edu_subject_actionMultiple(
      action: $action,
      ids: $ids,
      models: $models
   ) {
      ...ResultWithPayload
   }
}


