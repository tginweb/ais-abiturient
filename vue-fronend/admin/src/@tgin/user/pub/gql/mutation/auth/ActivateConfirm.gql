#import "@tgin/main/graphql/fragment/ResultWithPayload.gql"

mutation (
  $login: String,
  $sac: SecureActionConfirm,
  $lang: String
) {
  res: auth_ActivateConfirm(
    login: $login,
    sac: $sac,
    lang: $lang
  ) {
    ...ResultWithPayload
  }
}
