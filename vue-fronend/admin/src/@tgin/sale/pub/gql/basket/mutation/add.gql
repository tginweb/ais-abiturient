#import "@tgin/sale/core/gql/basket/fragments/Basket.gql"

mutation(
    $productId: Int,
    $productHash: String,
    $quantity: Float
)
{
  res: sale_pub_basket_Add(
    productId: $productId
    productHash: $productHash,
    quantity: $quantity
  ) {
      ...Basket
  }
}
