#import "@tgin/sale/core/gql/basket/fragments/Basket.gql"

mutation(
    $itemId: Int,
    $comment: String
)
{
  res: sale_pub_basket_comment(
    itemId: $itemId,
    comment: $comment
  ) {
    ...Basket
  }
}
