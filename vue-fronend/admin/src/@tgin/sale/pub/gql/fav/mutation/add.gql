#import "@tgin/sale/core/gql/fav/fragments/FavItem.gql"

mutation(
    $basketId: Int,
    $productId: Int
)
{
    res: sale_pub_fav_add(
        basketId: $basketId,
        productId: $productId
    ) {
        ...FavItem
    }
}
