#import "@tgin/sale/core/gql/vorder/fragments/Vorder.gql"

mutation(
    $op: String,
    $params: Json = null,
    $order: Json = null
)
{
  res: sale_pub_vorder_op(
    op: $op,
    params: $params,
    order: $order
  ) {
    ...Vorder
  }
}
