#import "@tgin/sale/core/gql/profile/fragments/OrderProfile.gql"

mutation(
    $id: Int,
    $name: String,
    $props: Json
)
{
    res: sale_admin_profile_save(
        id: $id,
        name: $name,
        props: $props
    ) {
        ...OrderProfile
    }
}
