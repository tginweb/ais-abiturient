#import "@tgin/main/graphql/fragment/Response.gql"

mutation(
  $id: Int,
  $action: String,
  $model: Json,
  $params: Json
)
{
  res: school_admin_contract(
    id: $id,
    action: $action,
    model: $model,
    params: $params
  ) {
    ...Response
  }
}
