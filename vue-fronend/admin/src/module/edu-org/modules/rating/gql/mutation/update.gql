#import "@tgin/main/graphql/fragment/ResultWithPayload.gql"

mutation(
    $id: String,
    $action: String,
    $model: JSON
) {
    res: edu_rating_admin_update(
        id: $id,
        action: $action,
        model: $model
    ) {
        ...ResultWithPayload
    }
}


