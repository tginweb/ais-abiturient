#import "../fragment/EduSSCompetitionListSnap.gql"
#import "@tgin/main/graphql/fragment/QueryInfo.gql"

query(
   $filter: JSON,
   $nav: QueryInputNav
) {
   res: edu_ssCompetitionListSnap_listRecordset(filter: $filter, nav: $nav) {
     info {
       ...QueryInfo
     }
     nodes {
       ...EduSSCompetitionListSnap
     }
   }
}
