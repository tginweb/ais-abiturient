#import "@tgin/main/graphql/fragment/ResultWithPayload.gql"

mutation(
  $id: JSON,
  $data: JSON
) {
   res: edu_order_admin_testsSave(
      id: $id,
      data: $data
   ) {
      ...ResultWithPayload
   }
}


