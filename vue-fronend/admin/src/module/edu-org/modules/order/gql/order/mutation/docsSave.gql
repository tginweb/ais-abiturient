#import "@tgin/main/graphql/fragment/ResultWithPayload.gql"

mutation(
    $id: JSON,
    $data: JSON
) {
    res: edu_order_admin__docs_save(
        id: $id,
        data: $data
    ) {
        ...ResultWithPayload
    }
}


