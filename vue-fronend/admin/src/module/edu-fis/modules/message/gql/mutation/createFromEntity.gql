#import "@tgin/main/graphql/fragment/ResultWithPayload.gql"

mutation(
  $messageType: String,
  $entityType: String,
  $ids: [String]
) {
   res: edu_fis_message_createFromEntity(
    messageType: $messageType,
    entityType: $entityType,
    ids: $ids
   ) {
      ...ResultWithPayload
   }
}

