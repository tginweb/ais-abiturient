type Mutation {
  edu_subject_admin_syncWithAis: ResultWithPayload

  edu_subject_actionMultiple(
    action: String
    ids: [Int]
    models: JSON
  ): ResultWithPayload

  edu_subject_action(
    action: String
    id: Int
    model: JSON
  ): ResultWithPayload
}

type EduSubject {
  actions: JSON
  adminActions: JSON
}
